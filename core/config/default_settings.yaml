# Nexus-Shell: System Defaults
# Location: core/config/default_settings.yaml
# These are the hardcoded engine defaults. Do not edit this file directly.
# Override these settings in ~/.config/nexus-shell/settings.yaml (Global)
# or .nexus.yaml (Project-level).

composition: vscodelike
tools:
  editor: nvim
  files: yazi
  chat: opencode
parallax:
  enabled: true
  pillars:
    - library
    - places
    - brains
    - projects
