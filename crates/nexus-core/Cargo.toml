[package]
name = "nexus-core"
version = "0.1.0"
edition = "2021"
description = "Shared types, traits, and constants for Nexus Shell crates"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
