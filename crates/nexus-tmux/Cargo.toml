[package]
name = "nexus-tmux"
version = "0.1.0"
edition = "2021"
description = "tmux adapter implementing the Mux trait for Nexus Shell"

[dependencies]
nexus-core = { path = "../nexus-core" }
serde_json = "1"
