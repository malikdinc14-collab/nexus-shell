# config/actions.yaml
# Global Action Registry for Nexus Shell
# Decouples the UI from specific shell commands.

actions:
  # Window Management
  restore: "restore"
  hard-reboot: "tmux kill-server"
  
  # Navigation
  focus_files: "$NEXUS_KERNEL/exec/focus.sh tree files"
  focus_editor: "$NEXUS_KERNEL/exec/focus.sh editor"
  focus_chat: "$NEXUS_KERNEL/exec/focus.sh chat"
  focus_terminal: "$NEXUS_KERNEL/exec/focus.sh terminal"
  quick_find: "$NEXUS_HOME/core/engine/search/quick_find.sh"

  # Shelf
  stow_pane: "shelf stow"
  recall_pane: "shelf recall"

  # Agents
  switch_model: "switch-model"
  set_role: "set-role"

# Identity Mappings
# Allows the dispatcher to find existing tabs in the stack.
identity:
  editor: "editor"
  explorer: "explorer"
  chat: "chat"
  terminal: "terminal"
